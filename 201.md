NATS、RabbitMQ、Kafka 這三者常被放在一起比,但其實設計哲學差很多,適合的場景也不太一樣。用你熟悉的角度(後端系統、資料工程)來拆解:

## 核心定位

**RabbitMQ** — 傳統 message broker,以 AMQP 協議為核心,強調靈活的路由(exchange/queue/binding 模型)。適合「任務分派」「服務間解耦」這類場景,訊息通常是短生命週期的工作項目。

**Kafka** — 分散式的 append-only log。本質上不是傳統意義的「佇列」,而是可重播的事件流。訊息持久化在 partition 裡,consumer 自己追蹤 offset。適合「事件溯源」「串流處理」「多個 consumer 各自消費同一批資料」的場景。

**NATS** — 輕量、極簡的 pub/sub 系統,核心設計目標是低延遲、高吞吐、簡單部署。NATS Core 本身不持久化(fire-and-forget),但 JetStream(NATS 的持久化層)補上了類似 Kafka 的 streaming 能力。

## 關鍵差異對照

| 面向 | RabbitMQ | Kafka | NATS (+ JetStream) |
|---|---|---|---|
| 訊息模型 | Queue-based,消費後即刪除 | Log-based,可重播,保留策略可調 | Core 是 pub/sub 廣播;JetStream 提供 stream + 持久化 |
| 路由彈性 | 非常強(exchange 型態多樣) | 弱(靠 partition key) | 中等(subject 階層式路由,類似 topic wildcard) |
| 吞吐量 | 中等 | 非常高(專為大量事件設計) | 高,延遲更低 |
| 運維複雜度 | 中等 | 高(ZooKeeper/KRaft、partition 管理) | 低(單一二進位檔,叢集設定簡單) |
| Consumer 模型 | Push,broker 主動推送 | Pull,consumer 自己管理 offset | 兩者皆可(Core 是 push,JetStream 可 pull) |
| 順序保證 | 單一 queue 內有序 | 單一 partition 內有序 | JetStream stream 內有序 |
| 生態成熟度 | 非常成熟,client library 齊全 | 業界標準,大數據生態(Kafka Connect、Streams)完整 | 較新,生態仍在成長,但 CNCF 專案 |

## 什麼時候選哪個

- **任務佇列 / RPC / 服務間解耦**,訊息處理完就丟:RabbitMQ 的 exchange 路由和 ack 機制很適合,尤其是 transactional outbox 這類「保證至少送達一次」的場景。
- **事件流、CDC、資料管線、需要重播歷史事件**:Kafka 是首選,尤其你要做 event sourcing 或多個下游系統各自獨立消費同一份事件流時,它的持久化 log 模型天然適合。
- **微服務間輕量通訊、需要低延遲、想要簡單運維**:NATS Core 很適合純訊息傳遞;如果需要持久化又不想扛 Kafka 的運維重量,JetStream 是不錯的折衷。NATS 也支援 request-reply,對做 RPC 風格的服務通訊很方便。

一個實務上常見的判斷點是「你需要重播歷史訊息嗎?」——如果需要(例如 debug、補跑、多個獨立 consumer group 各自處理進度),那基本上就是 Kafka 或 JetStream 的地盤,單純 RabbitMQ queue 做不到。如果只是需要「處理過就沒了」的任務分派,RabbitMQ 或 NATS Core 都比 Kafka 輕量、好維護。

如果你想深入某個特定面向(例如效能 benchmark、在 Rust 生態下各自的 client library 成熟度、或是跟你現有用 RabbitMQ + transactional outbox 的架構做比較),可以再告訴我,我可以再往下挖。

NATS 其實是分層架構,JetStream 是建立在 NATS Core 之上的一個模組,不是獨立的系統。可以理解成「NATS Core 負責傳遞,JetStream 負責記憶」。

## NATS Core:傳遞層

- 純粹的 pub/sub,fire-and-forget,沒有持久化。
- 訊息一旦發布,如果當下沒有 subscriber 在聽,就消失了。
- 極致輕量、低延遲(通常是微秒級),適合「即時性優先、可以接受偶爾漏訊息」的場景,例如服務發現、心跳、即時通知、request-reply 型的 RPC。
- 沒有 ack 機制,broker 不追蹤誰收到、誰沒收到。

## JetStream:持久化 + 串流層

在 NATS Core 之上加了一層,把 subject 的訊息寫進 **stream**(類似 Kafka 的 topic/partition 概念,但更彈性——一個 stream 可以吃多個 subject pattern)。負責的事情包括:

- **持久化**:訊息寫入磁碟,依 retention policy(依時間、依大小、依訊息數)保留。
- **重播能力**:consumer 可以從任意 offset 或時間點重新讀取歷史訊息,這是 Core 完全做不到的。
- **Consumer 管理**:支援 pull-based consumer(自己控制消費節奏)或 push-based,並且追蹤每個 consumer 的進度(類似 Kafka 的 consumer group + offset)。
- **At-least-once / exactly-once 語意**:透過 ack、去重(message ID + deduplication window)提供更強的送達保證。
- **Key-Value store 與 Object store**:JetStream 還衍生出 KV store 和 Object store 這兩個附加功能,底層用的是同一套 stream 機制。

## 分工邏輯

實務上你會這樣選:

- **只需要傳遞、不在乎漏訊息、要極致低延遲** → 直接用 NATS Core(例如 request-reply RPC、broadcast 通知)。
- **需要保證送達、需要重播、需要 consumer 進度追蹤** → 用 JetStream(例如事件驅動架構、CDC 消費、任務佇列且不能丟訊息)。

一個系統裡兩者常常並存——比如用 Core 做低延遲的即時查詢/回應,同時用 JetStream 處理需要可靠送達的事件流。這也是 NATS 相對 Kafka 的一個優勢:同一套 client library、同一個 server 部署,就能同時涵蓋「輕量傳遞」和「可靠串流」兩種需求,不用像 Kafka 生態那樣得另外疊 Kafka Connect 或搭配別的訊息系統做即時通訊。

兩個連結我看了,補充一下,順便更正一個小地方:

## 關於這兩個連結

**github.com/nats-io/jetstream** 這個 repo 其實已經是**封存(archived)狀態**,是 JetStream 早期開發階段的工具庫,README 裡也註明JetStream 在 NATS 2.2.0 正式 GA 後,文件已經移到核心 NATS 文件裡。所以現在要看 JetStream 的正式說明,應該看你貼的第二個連結(docs.nats.io),或是實作本體現在其實是在 `nats-server` 這個 repo 裡,不是這個獨立的 jetstream repo。

## docs.nats.io 上對 JetStream 的官方定義

這頁的說法跟我前面講的核心邏輯一致,但用詞更精準,值得對照:

官方把 Core NATS 和 JetStream 的差異定義得很乾脆:Core NATS 只會把訊息送給發布當下正在連線的訂閱者——最多送達一次,不會重播;JetStream 在此之上加了一層持久化,提供至少送達一次的保證,訊息在重啟後仍會存在,也可以重播。

另外有一個角度我之前沒特別強調,但官方文件講得很好——**JetStream 延伸的其實是「解耦的維度」**:Core NATS 本來就讓發布者跟訂閱者互相解耦,發布者不需要知道訂閱者是誰;JetStream 把這種解耦延伸到時間軸上——兩者不再需要同時在線。這句話其實把 JetStream 存在的意義講得很到位:Core 解決的是「空間上的解耦」(誰跟誰通訊不用互相知道),JetStream 解決的是「時間上的解耦」(不用同時上線)。

## 三個核心元件的關係

官方文件把 JetStream 拆成三塊,這跟我之前講的 stream/consumer 概念一致,但補充了細節:

- **Stream**:綁定一個或多個 subject 的伺服器端訊息儲存體,可設定用記憶體或磁碟儲存、保留策略(依限制或依 consumer ack 來清除訊息)、複寫份數等。
- **Consumer**:伺服器端、有狀態的 stream 視圖——伺服器會追蹤 client 消費到哪裡,應用程式端不需要自己維護進度。而且多個 consumer 可以各自獨立讀同一個 stream,每個都有自己的位置,這點跟 Kafka 的 consumer group 概念很像。
- **Client**:連到 consumer 接收訊息並 ack 的應用程式;一個 consumer 可以被多個 client 共用來分攤工作,每次 ack 都會推進 consumer 在 stream 裡的位置。沒在時限內 ack 的話伺服器就會重送,這正是 at-least-once 保證的來源。

值得注意的是官方文件也明確把 **Key-Value Store** 和 **Object Store** 列為建立在 stream 和 consumer 之上的更高層抽象——這兩者底層用的就是同一套 JetStream 機制,只是包裝成不同的 API 語意,這點跟你目前的 outbox pattern 場景可能有點關聯性(如果未來想拿 JetStream KV 做輕量的 idempotency/dedup 記錄,是同一套基礎設施)。
