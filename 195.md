# 全文模糊查詢複雜度比較:無索引 / PostgreSQL GIN / Elasticsearch

## 前情提要

理解「無索引」、「PostgreSQL 索引(主要是 GIN)」與「Elasticsearch(倒排索引)」在處理**全文模糊查詢**時的硬碟與 CPU 複雜度差距,核心在於「拿空間/預算換時間」的工程思維。

當我們談論「模糊查詢」時,通常指 `LIKE '%關鍵字%'`(子字串匹配)或全文檢索(Full-Text Search)。以下拆解這三者在硬碟(I/O)、CPU、以及**容易被忽略的寫入與運維成本**上的複雜度。

---

## 1. 無索引(No Index / Table Scan)

這就像是在沒有目錄、沒有索引的情況下,要求你在一萬本書裡找出包含「咖啡」這兩個字的所有頁面。

- **硬碟 I/O 複雜度:$O(N)$(極高)**
資料庫必須進行**全表掃描(Full Table Scan)**,把該資料表的所有資料區塊(Blocks)全部讀進記憶體。10GB 的表就得老老實實讀 10GB。

- **CPU 複雜度:$O(N \times M)$(極高)**
每一行都要用字串匹配演算法(KMP 或暴力法)從頭到尾檢查一遍,$N$ 是總字元數,$M$ 是關鍵字長度。實務上資料庫會用 `memchr`/SIMD 加速單行掃描,但整體仍是線性隨資料量成長。

- **寫入成本:$O(1)$**
沒有索引要維護,這是它唯一的優勢 — 適合寫多讀少、且讀取頻率低到可以忍受全表掃描的場景。

---

## 2. PostgreSQL 索引(以 GIN 全文檢索為例)

PostgreSQL 處理全文檢索通常使用 **GIN(Generalized Inverted Index,通用倒排索引)**。它把文章拆成一個個標記(Tokens/Words)。

> **修正常見誤解:** GIN 本身**就是倒排索引**,不是「類似 B-Tree 的東西」。正確結構是兩層:
> 1. 外層是一棵 B-Tree,鍵值是不重複的詞條(lexeme)
> 2. 每個詞條下掛一份 posting list(低頻詞,壓縮成連續整數陣列)或一棵 posting tree(高頻詞如虛詞,list 太大就升級成樹)
>
> 所以 PG 和 ES 底層資料結構其實是**同一個家族**,差別在工程實作精細度,不是本質不同。

- **硬碟 I/O 複雜度:$O(\log N)$ 到 $O(K)$(中等)**
先讀索引找到匹配的 TID(Tuple ID)列表,再離散地(Random I/O)回表抓資料。

- **⚠️ 隱藏成本:MVCC 可見性檢查**
這是 PG 全文檢索最容易被低估的痛點。GIN 索引裡存的 TID 不保證該筆資料對當前交易可見(可能已被其他交易刪除或更新)。除非 Visibility Map 標記該頁全部可見,否則**每一筆命中都要額外回堆(heap)做一次可見性檢查**,這是 GIN 難以支援 Index-Only Scan 的主因,也是隨機 I/O 壓力的主要來源。

- **⚠️ 隱藏成本:寫入放大**
如果每次 INSERT/UPDATE 都即時更新 GIN 索引,單筆寫入代價很高(要更新多個詞條的 posting list)。PostgreSQL 用 `fastupdate`(pending list)機制緩解:新資料先塞進一個未排序的暫存區,累積到一定量或觸發 VACUUM 才批次合併進正式索引結構。這犧牲了「新資料可搜尋的即時性」換取寫入吞吐量。

- **模糊查詢限制:** `LIKE '%關鍵字%'`(前後模糊)標準 GIN 用不上,必須搭配 `pg_trgm`(三元組索引),索引體積會明顯膨脹。**若是中文/CJK 內容**,標準 `english`/`simple` 分詞器無法正確斷詞,通常需要 `zhparser` 或 `pg_bigm` 做 bigram 切分,否則模糊查詢等同全表掃描。

- **CPU 複雜度:$O(\log N)$(中等)**
排名算法(`ts_rank`)在 SQL 執行引擎內計算,缺乏針對大規模文字特化的優化(如 SIMD)。

---

## 3. Elasticsearch(基於 Lucene 的倒排索引)

- **硬碟 I/O 複雜度:接近 $O(1)$ 或 $O(K)$(極低)**
Term Dictionary 透過 **FST(Finite State Transducer)** 高度壓縮並常駐記憶體。Posting List 用 Frame of Reference / Roaring Bitmap 壓縮,硬碟 I/O 被降到最低。

- **CPU 複雜度:$O(K)$ 且有硬體加速(極低)**
多關鍵字交集/聯集用 Bitset 位元運算,可利用 SIMD 指令集加速。排名用原生優化過的 **BM25**(ES 5.0 起取代舊版 TF-IDF),相關性計算成本遠低於 PG。

- **架構上的關鍵差異:不可變的 Segment(Immutable Segments)**
Lucene 的段(segment)一旦寫入就不可變更,更新/刪除是靠標記 tombstone + 定期 merge 段來實現,而非原地修改。這是它能做到極致查詢效能的根本原因,但代價是:
- **寫入不是即時可搜尋**:預設 `refresh_interval` 為 1 秒,真正的 Near-Real-Time,不是 Real-Time。
- **Segment Merge 是持續性背景成本**,會消耗額外 CPU 與硬碟 I/O,調參不當容易造成查詢延遲抖動。

- **⚠️ 容易被低估的運維與空間成本**
- 儲存空間常是原始資料的 2~3 倍以上(倒排索引 + doc values + stored fields + 各種快取結構同時存在)。
- JVM Heap 管理與 GC 停頓是叢集穩定性的常見痛點。
- 需要獨立的叢集運維能力(shard 規劃、node 擴縮容、副本策略),不像 PG 索引是資料庫內建的一部分,近乎「零運維增量」。

---

## 綜合比較表

| 維度 | 無索引 | PostgreSQL GIN | Elasticsearch |
|---|---|---|---|
| 硬碟 I/O(讀) | $O(N)$ | $O(\log N)$ + MVCC 回表 | 接近 $O(1)$ |
| 硬碟空間 | 無額外消耗 | 資料量的 0.5~2 倍 | 資料量的 2~3 倍以上 |
| CPU(讀) | $O(N \times M)$ | $O(\log N)$ | $O(K)$,SIMD 加速 |
| 寫入成本 | $O(1)$ | 中(有 pending list 緩衝) | 低即時性(近即時,靠 segment merge) |
| 中文/CJK 支援 | 天然可用(全表掃描不在乎語言) | 需額外裝 `zhparser`/`pg_bigm` | 需搭配 IK/smartcn 等分詞外掛 |
| 運維複雜度 | 無 | 低(資料庫內建功能) | 高(獨立叢集、JVM 調參) |
| 適用規模 | 小表(百萬列以下、低頻查詢) | 千萬級,單純全文檢索 + 不想多維運一套系統 | 億級,複雜關聯、模糊搜尋、高查詢併發 |

---

## 用偽代碼理解執行流程

比起複雜度符號,直接看「I/O 發生在哪一行」通常更直觀。三段偽代碼刻意用同樣的變數命名,方便對照。

### 1. 無索引:全表掃描

```
function full_table_scan(table, pattern):
    results = []
    for block in table.disk_blocks:        # O(N) 個區塊,每個都得讀 —— 硬碟 I/O 發生在這裡
        page = read_from_disk(block)
        for row in page.rows:
            if pattern in row.text:         # O(M) 逐字元比對 —— CPU 成本發生在這裡
                results.append(row)
    return results
```

沒有分支、沒有捷徑,I/O 和 CPU 都跟資料總量綁死。

### 2. PostgreSQL GIN:索引查找 + 回堆驗證可見性

```
GIN_Index = BTree<lexeme, PostingList | PostingTree>   # 兩層結構:B-Tree 詞條 → posting

# ---- 寫入路徑 ----
function insert_row(row):
    tokens = tokenize(row.text)             # 中文需 zhparser / pg_bigm,否則斷詞失敗
    for token in unique(tokens):
        pending_list.append((token, row.tid))  # fastupdate:先丟暫存區,不即時更新主索引
    # 背景 VACUUM 或達到閾值時,才批次合併進 GIN_Index —— 這是「寫入放大被延後攤銷」的機制

# ---- 查詢路徑 ----
function query(pattern):
    tokens = tokenize(pattern)
    candidate_tids = []
    for token in tokens:
        postings = GIN_Index.lookup(token)  # O(log N) B-Tree 查找,通常在 shared_buffers 快取內
        candidate_tids = intersect(candidate_tids, postings)

    results = []
    for tid in candidate_tids:
        row = heap_fetch(tid)                # ⚠️ 隨機 I/O:回堆抓實際資料列
        if not visibility_map.is_all_visible(tid.page):
            if not mvcc_visible(row, current_txn):  # ⚠️ 額外檢查:這行是原文完全沒提到的隱藏成本
                continue                      # 該筆資料對本交易不可見(被其他交易刪除/更新),丟棄
        results.append(row)
    return results
```

關鍵:索引只負責「縮小候選範圍」,**真正的資料正確性驗證要回堆才算數**。這是 GIN 比 ES 多出來的一層工。

### 3. Elasticsearch / Lucene:記憶體詞典 + 不可變 segment

```
TermDict = FST<term, posting_list_pointer>   # 高度壓縮,常駐記憶體

# ---- 寫入路徑 ----
function index_document(doc):
    tokens = analyze(doc.text)                # standard / ik / smartcn 分詞器
    new_segment = build_in_memory_segment(tokens, doc.id)
    write_to_disk(new_segment)                # 產生新的、不可變的 segment,不修改舊資料
    # 預設 refresh_interval = 1 秒後才可被搜尋到 —— Near-Real-Time,不是 Real-Time

    if segment_count > threshold:
        background_merge_segments()           # 背景合併小 segment,順便清掉 tombstone 標記的刪除文件

# ---- 查詢路徑 ----
function query(pattern):
    tokens = analyze(pattern)
    result_bitmap = null
    for token in tokens:
        pointer = TermDict.lookup(token)      # 記憶體查找,近乎 O(1)
        posting_list = decompress(read(pointer))  # Frame-of-Reference / Roaring Bitmap 解壓
        bitset = to_bitset(posting_list)
        result_bitmap = bitwise_and(result_bitmap, bitset)  # SIMD 加速位元運算,無 heap_fetch 這道手續

    docs = []
    for doc_id in result_bitmap.set_bits():
        score = bm25_score(doc_id, tokens)    # 原生優化過的相關性計算
        docs.append((doc_id, score))
    return sort_by_score(docs)
```

關鍵:**沒有 `heap_fetch` 和 `mvcc_visible` 這兩步**。因為 segment 一旦寫入就代表「當下已提交的事實」,查詢時不需要額外驗證可見性 —— 這是 ES 讀取路徑比 PG 少一層開銷的根本原因,但代價是寫入永遠有 1 秒左右的延遲(refresh_interval),而且 `background_merge_segments()` 這行是持續性背景成本,不會出現在查詢延遲的複雜度公式裡,卻實實在在吃 CPU 和硬碟頻寬。

### 三段偽代碼的對照重點

| 觀察點 | 無索引 | GIN | ES/Lucene |
|---|---|---|---|
| 資料筆數是否直接影響迴圈次數 | 是(每筆都掃) | 否(只掃候選) | 否(只掃候選) |
| 查詢路徑是否需要「回頭驗證」 | 不需要(掃到就是真的) | **需要**(`mvcc_visible`) | 不需要(segment 即真相) |
| 寫入是否立即反映在索引 | N/A | 否(pending list 延後) | 否(refresh_interval 延後) |
| 背景維護成本是否存在 | 無 | VACUUM 合併 pending list | segment merge |

## 一句話總結

- **無索引**:沒做功課,考試時把整本書翻一遍(高 I/O、高 CPU、零維護成本)。
- **PostgreSQL GIN**:做了課後字典索引,翻字典找頁碼再去翻書 — 但每次翻到書頁還得確認這頁「現在還算不算數」(MVCC 可見性檢查)(低 I/O、中 CPU、中維護成本)。
- **Elasticsearch**:把整本書背進大腦,關鍵字做成導圖,一想就能精確定位 — 但代價是要養一個記憶宮殿(叢集),而且它記的東西是「一秒前」而非「當下」(極低 I/O、極低 CPU,但高儲存與運維成本)。

## 實務選型建議

- 資料量在千萬列以內、查詢頻率不高、想避免多維運一套系統 → **PostgreSQL + GIN/pg_trgm** 通常夠用,尤其如果你的資料本來就在 PG 裡,不需要額外做 CDC 同步。
- 中文內容用 PG 全文檢索,記得裝 `zhparser` 或至少用 `pg_bigm`,否則預設分詞器對中文基本無效。
- 查詢併發高、需要複雜相關性排序、資料量上億、或本來就有能力維運 JVM 叢集 → **Elasticsearch**,但要把 2~3 倍儲存空間和 segment merge 的背景 I/O 成本算進容量規劃。
- 折衷方案可以考慮 **ParadeDB(pg_search)** 或 **ZomboDB**:在 PostgreSQL 裡直接嵌入 BM25/Lucene 風格的索引,兼顧「資料在 PG 裡」與「搜尋效能接近 ES」,值得評估但生態成熟度不如原生 ES。
