[這篇文章講的是一個很典型但容易被誤判的問題:**Rust 服務沒有記憶體洩漏,但 RSS 就是降不下來**,根本原因出在記憶體配置器(allocator)身上。](https://pranitha.dev/posts/rust-and-memory-allocators/)

## 背景與現象

作者的服務是事件驅動架構:
- 從訊息佇列(Kafka/Redis Streams/NATS)讀事件
- 每個事件 spawn 一個 Tokio task 處理
- 用 `Semaphore` 限制併發數做背壓控制

工作負載是**突發性(bursty)且稀疏**的:每小時 3-4 次高峰,每次約 10 萬個事件。問題是:每次高峰過後,記憶體用量預期應該掉回基準線,但 K8s 看到的 RSS 卻卡在高點不下降。

## 排查:是洩漏嗎?

用 `dhat` 做 heap profiling,結果很關鍵:
- 高峰時(`t-gmax`)heap 用了約 1.4GB
- 程式結束時(`t-end`)只剩 10KB

這證明 Rust 程式**幾乎把所有東西都正確釋放了**,不是邏輯上的洩漏。但 K8s 看到的 RSS 卻沒反映這件事——這個落差,來自 glibc 配置器管理已釋放記憶體的方式。

## glibc (ptmalloc) 的問題所在

這是全文核心,用你熟悉的角度講就是個**堆疊/連續區域回收**問題:

1. **Arena 結構**:glibc 用 arena 管理記憶體,每個 arena 是一塊連續的堆積區域。Tokio 的 work-stealing 會讓不同 task 的配置散落在不同 arena、且彼此交錯排列,沒有明確邊界。

2. **回收只能從頂端(top chunk)往下修剪**:OS 只能回收 arena 頂端連續的空閒空間。如果某個還存活的配置(比如 Task A 的 Buffer)卡在中間偏上的位置,它就像個「門栓」,把它下面所有已釋放的記憶體都鎖住,OS 拿不回去。

3. **更微妙的一點**:作者的場景裡其實**沒有長期存活的配置**在擋路,但 RSS 還是掉不下來。原因是最後幾個被釋放的 chunk 進了 `tcache`/`bins` 快取,而沒有觸發整併(consolidation)回到可回收的頂端空間。因為突發之後沒有新的配置活動去觸發整併,這些快取就一直卡著,記憶體就這樣被「困」在配置器裡而非還給 OS。

4. Thread arena 用 mmap 掛載的 sub-heap 讓情況更糟:新 sub-heap 掛上去、top pointer 移過去後,如果新 sub-heap 裡還有沒整併的 free chunk,舊 sub-heap 即使全空也可能繼續佔著。

驗證方法很直接:對正在跑的程序呼叫 `malloc_trim(0)`(用 gdb attach 進去呼叫),記憶體立刻掉回基準線——證實問題確實在配置器層,而非應用邏輯。但因為服務是事件驅動、沒有乾淨的時機點可以安全呼叫 `malloc_trim`,所以這只能當診斷工具,不能當生產環境的解法。

## 換成 jemalloc:解決了

```rust
#[global_allocator]
static GLOBAL: tikv_jemallocator::Jemalloc = tikv_jemallocator::Jemalloc;
```

jemalloc 用的是**按大小分類(size-class)的獨立 slab**,而不是連續 arena:

```
jemalloc arena
├── slab: 256B  → 只放 256B 的區塊
├── slab: 8KB   → 只放 8KB 的區塊
```

關鍵差異:一個 slab 內的所有 slot 都釋放後,那一頁就是**完全獨立地**被標記為 dirty,背景執行緒呼叫 `madvise` 還給 OS——完全不受其他 size class(比如 8KB slab)裡是否還有存活配置影響。glibc 那種「一個活著的 chunk 卡住整條」的問題,在 jemalloc 裡結構性地不會發生。

搭配設定:
```
background_thread: true
dirty_decay_ms: 10000
muzzy_decay_ms: 0
```
背景執行緒依照 decay timer 主動清理,不需要等應用層有新配置活動才觸發。

## 為什麼 mimalloc 反而不適合他們的場景?

這段也很值得注意,尤其你的場景涉及 Tokio work-stealing:

- mimalloc v3 對於「跨執行緒釋放」(cross-thread free,也就是 Thread A 配置、Thread B 因為 work-stealing 執行/釋放)的處理方式,是把釋放的區塊丟到該 page 的原生 thread 的 `xthread_free` atomic list,**不會立即**進入正常的 local free list。
- 這個 list 要等擁有該 page 的執行緒之後**又有配置活動**時才會被整併(reconcile)。
- 因為他們的負載是稀疏的,高峰過後所有 worker 都會 idle 下來。而 Tokio 喚醒 idle worker 時是從 sleepers list 裡 pop 一個,不是輪流全部喚醒——所以生產環境裡零星的 health check / metrics 配置活動,只會喚醒少數固定的幾個 worker,無法觸發全體 worker 的整併。
- 就算調整 `MIMALLOC_PURGE_DELAY=0`、`MIMALLOC_PURGE_DECOMMITS=1`,這些設定只影響「已經被判定為 free 的記憶體」何時歸還 OS,並不會強制去 reconcile 那些卡在 `xthread_free` 裡的 pending frees。

## 結論一句話總結

三種配置器對比:
- **glibc**:預設夠好,但突發式負載下,arena 重用 + 碎片化 + 快取的 free chunk 會讓記憶體卡住不還。
- **mimalloc**:高吞吐低延遲很強,但回收機制綁定在配置活動上——worker 常態性 idle 的場景反而是它的弱點。
- **jemalloc**:背景執行緒依 decay timer 主動 purge,不需要靠應用層配置活動觸發,最適合這種「突發後長時間 idle」的工作模式。

---


## musl 的 malloc(mallocng)長什麼樣子

musl 目前使用的配置器叫 **mallocng**(musl 1.2.x 之後,取代舊版類 dlmalloc 的實作)。它的設計理念其實跟文章裡的 jemalloc 更像,而不是跟 glibc 的 ptmalloc 像:

- **按大小分類的 group/slot 結構**:mallocng 把配置依大小分成不同 size class,每個 class 用固定大小的「group」(一組 mmap 出來的頁面)來裝同大小的物件,概念上類似 jemalloc 的 slab。
- **沒有 glibc 那種「單一連續 arena、只能從頂端修剪」的限制**:一個 group 裡的所有 slot 都釋放完後,整個 group 可以獨立被 `munmap` 掉,不需要等鄰居也空出來、不需要頂端指標往下推。

所以理論上,mallocng **確實不會有 glibc 文章裡那種「一個活著的 chunk 卡住底下一大片已釋放記憶體」的結構性問題**,這部分你的理解方向是對的。

## 但這句話有兩個地方需要修正

**1. musl 不保證「及時」歸還——沒有背景執行緒 + decay timer**

jemalloc 之所以能解決文章裡的問題,關鍵不只是「size-class 分離」,而是它有:
```
background_thread: true
dirty_decay_ms: 10000
```
**背景執行緒主動依照 timer 去 purge**,不需要等應用層有新的配置活動觸發。

musl 的 mallocng **沒有這種主動背景回收機制**。一個 group 要被 `munmap`,通常還是要等到「釋放發生時」順帶觸發回收檢查——換句話說,回收時機仍然跟配置/釋放活動綁定,跟文章裡 mimalloc 遇到的問題(worker idle 之後沒有活動去觸發 reconcile/purge)是類似的風險。如果你的服務也是突發後長時間 idle,理論上 musl 一樣可能出現「最後一批釋放沒有後續動作去觸發歸還」的情況,只是機率跟嚴重程度可能比 glibc 低,因為它不會被單一存活物件卡住一整片。

**2. 切換到 musl target 帶來的不只是 malloc 差異**

- **靜態連結**:musl 通常搭配 static musl build(`x86_64-unknown-linux-musl`),這會影響 binary 大小、部署方式,跟 glibc 動態連結是不同的運維模型。
- **多執行緒吞吐效能**:musl malloc 在高併發、多執行緒重度配置場景下,historically 效能不如 glibc 或 jemalloc(雖然 mallocng 已經改善很多,但沒有 jemalloc 那種針對多核調校的成熟度)。
- **DNS 解析等行為差異**:musl 的 NSS/DNS 解析行為跟 glibc 不同,如果服務有網路依賴要注意。

## 小結

如果要精準表達,應該說:

> 換成 musl target 是另一條路,因為 musl 的 mallocng 用 size-class 分離的 group 結構,不像 glibc 那樣受限於「只能從 arena 頂端修剪」,結構上不會被單一存活物件卡住整片記憶體。但它沒有 jemalloc 那種背景 decay-based 主動 purge 機制,回收時機仍依賴配置/釋放活動觸發,所以在「突發後長時間 idle」的工作模式下,能否「及時」歸還仍不是保證的,而且切換 musl target 還牽涉靜態連結、效能特性等更大範圍的改動。

如果目標單純是「歸還記憶體給 OS 這件事」,文章裡驗證過的 jemalloc + `background_thread` + decay timer 組合,是針對這個具體問題更直接對症的解法;musl 是一個可能有效但沒有明確保證、且連帶影響更廣的替代方案。
